# S3-Benchmarks 2.2.2 Runtime Cost Attribution and Frontier Lab

## 1. Executive Summary

The campaign isolated the first causal candidate without changing S3. The
pinned O0/O1 assembly was regenerated byte-identically, and a fail-closed
benchmark-side rewrite removed only the exact instruction-budget accounting
sites. Static audit passed. The causal native experiment remains incomplete
because the configured Linux guest was unavailable; no diagnostic timing or
speed claim is made.

## 2. Starting Evidence

The prior XSBench protocol established a reproducible, workload-local gap with
`K=750000`, zero inline warmups, five external warmups, 30 repetitions, and
independent E/F sessions. The largest E/F median delta was 1.404480036%.

## 3. PR20 Reconciliation

PR #20 remains OPEN, DRAFT, MERGEABLE, and unmerged at `7b84d90`. Its body was
updated to record the warmup-budget confounder, selected K, E/F closure, and
the preserved A/B/C and D=NOT_RUN history. The body update did not change git
HEAD.

## 4. Baseline Artifact Reproduction

`BASELINE_ASSEMBLY_REPRODUCTION=PASS`: regenerated O0 and O1 assembly matched
the frozen files byte-for-byte. Frozen shared-object hashes remain recorded,
but rebuilding `.so` files was `NOT_RUN` because the Linux guest was
unreachable from this host.

## 5. S3 Runtime Safety Audit

The input contains instruction-budget and frame-budget machinery plus bounds,
initialization, helper, call, and ABI paths. This campaign did not remove or
alter any production mechanism.

## 6. Static Assembly Analysis

O0 contains 521 exact instruction-budget sites and O1 contains 520. Both have
three frame-limit sites. Calls and stack-operation approximations are retained
by the rewrite. Full counts are in `RUNTIME_SAFETY_ENVELOPE_AUDIT.md` and the
JSON result.

## 7. Instruction Budget Instrumentation

The diagnostic rewriter removes only the four-line `movabs/cmp/jae/inc`
sequence at the pinned 10-billion limit. It rejects partial or unknown
layouts. O0 and O1 reports each show zero unexpected mutations.

## 8. Diagnostic Artifact Construction

Diagnostic `.s` files and hashes were produced. Diagnostic `.so` files were not
produced, so these are not production artifacts and cannot be timed yet.

## 9. Correctness Gates

Historical baseline TINY/SMALL/MEDIUM hosted/native correctness remains PASS.
The diagnostic native correctness gate is NOT_RUN, not inferred from static
equivalence. The benchmark repository full suite passed `59 passed, 1 skipped,
0 failed` at the pre-commit working-tree HEAD recorded in the transcript.
`compileall` and the evidence registry check also passed.

## 10. Run G / 11. Run H

Both are NOT_RUN. The required Linux environment was unavailable; E/F data was
not reused for causal attribution.

## 12. Reproducibility

G/H reproducibility is NOT_RUN. The existing E/F reproducibility result remains
historical evidence only.

## 13. Causal Attribution / 14. Residual Gap

`INSTRUCTION_BUDGET_CAUSALITY=UNDER_TEST`. No ratio, residual gap, or excess
time attribution is mathematically reportable until the diagnostic native
artifact is built and timed.

## 15. Frame Attribution

Deferred. The residual threshold cannot be evaluated without G/H.

## 16. Remaining Pressures

Frame bookkeeping, bounds, initialization, indexing, stack traffic, register
pressure, calls, and helper overhead remain open. No one is promoted to cause.

## 17. Pressure Map V9

See `PRESSURE_MAP_V9.md` and `.json`. The map deliberately distinguishes
observed structure from causal evidence.

## 18. Relation to S3 PR #190

PR #190 is an open conflicting Draft register/memory policy experiment. The
current residual cannot be compared to it until G/H produce a valid causal
residual. Classification: `UNKNOWN`; no S3 mechanism was enabled.

## 19. Frontier Lab State

The initial eight-file Frontier Lab structure is under `research/frontier-lab/`.
It contains state, ledger, pressure map, scorecard, opportunities, GitHub
intelligence, and an external radar. No new workload pilot was started.

## 20. GitHub Intelligence

PRs #15 through #20 form an open Draft dependency chain. Stack depth is 7 with
the current branch; stack debt is HIGH. No consolidation was performed.

## 21. External Frontier Radar

The radar records 16 candidates spanning embedded, scientific, graph,
correctness, and compiler-fuzzing domains. It is a planning watchlist, not an
adoption claim.

## 22. Scorecard

Evidence Score V1 is 61/100 under the explicit rubric. No before/after delta is
claimed because the prior campaign did not use this rubric.

## 23. Opportunity Map

22 meaningful evidence opportunities are recorded. The immediate next action
is restoring the Linux guest and completing the already-defined G/H experiment.

## 24. New Pilot

`NEW_FRONTIER_PILOT=NONE`. The campaign did not start a second active causal
experiment.

## 25. Next Three Campaigns

1. Restore Linux native execution and complete G/H for instruction-budget attribution.
2. If residual exceeds the predeclared 5x threshold, isolate frame bookkeeping.
3. Validate the resulting safety-cost classification on one regular numerical,
   one irregular, and one control-flow workload before any S3 proposal.

## 26. Open Questions

- What is the native timing ratio after budget instrumentation is removed?
- Does the residual exceed the frame-attribution threshold?
- Which residual pressure generalizes beyond XSBench?
- Can the safety envelope be redesigned without losing bounded execution?

## 27. Final Decision

The diagnostic hypothesis remains under test. The campaign is not ready for
merge, production promotion, tag, release, or S3 changes. `S3_SOURCE_CHANGED=NO`.

Full-suite transcript SHA-256:
`5594570eeb3123fd912881682a42ddb8ece363364eb54cc2e506646bbbb70892`.
